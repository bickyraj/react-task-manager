{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install && npm run build)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4",
    "react-router-dom": "^6.14.2",
    "tailwindcss": "^3.3.3"
  }
}
